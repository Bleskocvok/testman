name: Hello Win
run-name: ${{ github.actor }} is testing stuff
on:
  - push
jobs:
  hello-win:
    runs-on: windows-latest
    steps:
      - run: windres -V

  another-try:
    runs-on: windows-latest
    steps:
      - run: windres.exe -V
